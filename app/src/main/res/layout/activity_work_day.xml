<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_work_day"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="80dp"
    tools:context=".WorkDayActivity"
    android:background="@color/Primary">

    <LinearLayout
        android:id="@+id/TopBarWorkDay"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:theme="@style/Theme.Kopi_Hawa.AppBarOverlay"
        tools:ignore="MissingConstraints"
        android:orientation="horizontal"
        android:paddingLeft="10dp"
        android:paddingTop="8dp"
        android:background="@color/orange">


        <ImageButton
            android:id="@+id/btnBackWorkDayActivity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/activity_vertical_margin"
            android:contentDescription="@string/btnBackWorkDay"
            app:srcCompat="@drawable/ic_baseline_arrow_back_24" />

        <TextView
            android:layout_width="269dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/activity_horizontal_margin"
            android:gravity="center_horizontal"
            android:text="@string/WorkDayActivityText"
            android:textAlignment="center"
            android:textColor="@color/Primary"
            android:textSize="25dp"
            android:textStyle="bold"/>


    </LinearLayout>

    <ImageView
        android:layout_width="350dp"
        android:layout_height="300dp"
        android:background="@color/Primary"
        android:layout_margin="30dp"
        app:layout_constraintBottom_toBottomOf="@+id/activity_work_day"
        app:layout_constraintTop_toBottomOf="@+id/TopBarWorkDay"
        app:layout_constraintStart_toStartOf="@+id/activity_work_day"
        app:layout_constraintEnd_toEndOf="@+id/activity_work_day"/>


</androidx.constraintlayout.widget.ConstraintLayout>